# Novel Team Acceptance Tests

These fixtures define behavioral invariants, not a single preferred piece of prose.

- Run `scripts/verify-implementation.py` for deterministic project, skill, scene-index, installation-parity, and tool checks.
- Use `acceptance-prompts.md` with fresh isolated agents for semantic forward-tests.
- Record dated forward-test results in `tests/results/`, including files consulted, pass/fail decisions against `fixtures/acceptance-cases.yaml`, and unresolved issues.
